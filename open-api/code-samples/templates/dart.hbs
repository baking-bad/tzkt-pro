import 'package:postgrest/postgrest.dart';

var client = PostgrestClient('{{client_url}}');
var response = await client
    .auth('{{auth}}')
    .from('{{table_name}}')
    .select('*')
    .limit(5)
    .end();
