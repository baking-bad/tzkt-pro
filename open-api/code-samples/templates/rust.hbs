use postgrest::Postgrest;

let client = Postgrest::new("{{client_url}}");
let resp = client
    .auth("{{auth}}")
    .from("{{table_name}}")
    .select("*")
    .limit(5)
    .execute()
    .await?;
