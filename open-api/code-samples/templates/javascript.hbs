import { PostgrestClient } from '@supabase/postgrest-js'

let client = new PostgrestClient('{{client_url}}')
let { body: {{table_name}} } = await client
  .auth('{{auth}}')
  .from('{{table_name}}')
  .select("*")
